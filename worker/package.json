{
  "name": "social-report-agent-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "node scripts/migrate-d1.mjs --local",
    "db:migrate:remote": "node scripts/migrate-d1.mjs --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trpc/server": "^11.0.0",
    "nanoid": "^5.0.7",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.4.2",
    "wrangler": "^3.40.0"
  }
}
